@model List<EsselTestLocalApplication.Models.SamplePicture>
@if (Model.Count > 0)
{ 
    <style>
        /*img {
            max-width: 100%;
            max-height: 100%;
        }*/
        .imgcls {
            width: 200px;
            height: 400px;
            object-fit: cover;
        }

        .portrait {
            height: 80px;
            width: 30px;
        }

        .landscape {
            height: 30px;
            width: 80px;
        }

        .square {
            height: 75px;
            width: 75px;
        }
    </style>
<table id="DocumentTable" class="table order-list doctable dataTable">
        <thead>
            <tr>
               
                <td style="text-align:center;color:#fff !important">Name</td>
                <td style="text-align:center;color:#fff !important">Preview</td>           
                <td style="text-align:center;color:#fff !important"></td>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
            <tr>
                <td style="text-align:center">
                    <img src="@item.Path"/>
                </td>
                <td style="text-align:center">
                    <img src="data:image;base64,@item.DocBinaryData" />
                    @*<img src="data:image;base64,@item.DocBinaryData" />*@

                </td>
            </tr>
                    }
</tbody>

    </table>
}
<script>
    @*$("#docimg").src = String.Format(@"data:image/jpeg;base64,{0}",base64string);*@
</script>
