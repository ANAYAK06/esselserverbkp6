
@using System.Data
@model DataTable
@*@{
        ViewBag.Title = "SampleDynamicView";
        Layout = "~/Views/Shared/EsselMaster.cshtml";
    }
    <h2>SampleDynamicView</h2>*@


<div class="table-responsive col-md-12">
    @if (Model.Rows.Count > 0)
    {
        <table class="hover stripe nowrap example dataTable no-footer" id="tblIndividualLedgerDetails">
            <thead>
                <tr>
                    @foreach (System.Data.DataColumn col in Model.Columns)
                    {
                        <th style="width:10%">@col.Caption</th>
                    }
                </tr>
            </thead>
            <tbody>
                @foreach (System.Data.DataRow row in Model.Rows)
                {
                    <tr>

                        @foreach (var cell in row.ItemArray)
                        {
                            
                            <td><span style="max-width:10px !important;">@cell.ToString() </span></td>
                        }
                    </tr>
                }
            </tbody>
        </table>
    }
    else {

        <div class="text-center"><span>No Data Found</span></div>
    }
</div>
<script type="text/javascript">
    $(document).ready(function () {
        var IndledgerGrid = null;
        InitializeIndLedgerGrid();
    });


    function InitializeIndLedgerGrid() {
        IndledgerGrid = $('#tblIndividualLedgerDetails').DataTable({
            destroy: true,
            paging: false,
            lengthChange: false,
            searching: false,
            lengthChange: false,
            info: false,
            paging: false,
            language: {
                "emptyTable": "No Data Found"
            },          
            "initComplete": function (settings, json) {

            },
            "createdRow": function (row, data, index) {
                //var date = $('td:eq(0)', row).html();
                //var trimStr = $.trim(date);
                //var typeArr = trimStr.split(' ');
                //var d3 = typeArr[0];
                //var t1 = "'" + d3 + "'";
                //var bldate1 = moment(t1, "DDMMYYYY").format('DD-MMM-YYYY');
                //$('td:eq(0)', row).html(bldate1);
                //alert(date);
            },
        });
    }

</script>
